# Telora 仕様書（現行仕様）

無料・クロスプラットフォーム（Mac / Windows）の動画編集ソフト **Telora**。
Electron + システム FFmpeg で動作し、テロップ（字幕）挿入とカット編集を中心に、
Filmora / Premiere のような直感的なタイムライン編集を行う。

> このドキュメントは「現在の実装が満たしている仕様」を明文化したもの。挙動を変える際はここも更新する。

---

## 1. 全体構成

- **メインプロセス**（`src/main/`）: ウィンドウ生成、メニュー、ファイル入出力、FFmpeg 実行（書き出し・メタ取得・フレーム抽出）、Whisper 文字起こし。
- **プリロード**（`src/main/preload.js`）: `contextBridge` で `window.api` を公開（IPC 経由の安全な橋渡し）。
- **レンダラ**（`src/renderer/`）: バニラ JS（ES Modules）。state / preview / timeline / inspector / media / edit / export-ui / import-* に分割。
- 状態は `state.js` の中央ストア＋イベントバス（`on/emit`）＋履歴（undo/redo）で管理。

---

## 2. トラックモデル

- トラックは **2 種のみ**:
  - `visual`: 動画・画像・テロップを **種別に縛られず自由に**配置できる（画面の上半分の領域）。
  - `audio`: 音声クリップ（画面の下に配置）。
- トラック配列の **先頭が最前面（上のレイヤ）**。`visual` は上へ、`audio` は下へ積まれる。
- 最下段の `visual` トラックが **ベーストラック**（`base: true`）。主映像であり、書き出しの「背景連結」の基準になる。削除不可。
- **初期状態**: `visual` 3 層（V3 / V2 / V1=base）＋ `audio` 1 層（A1）をあらかじめ表示。
- クリップ種別（`clip.kind`）: `video` / `image` / `text`（テロップ）/ `audio`。
  - 映像系（video/image/text）は `visual` トラックのみ、`audio` は `audio` トラックのみに置ける。

---

## 3. 自動レイヤー（柔軟な配置ルール）

「置こうとした場所が埋まっていれば、自動的に上の層へ」を基本方針とする。

- **ライブラリ（ビン）から追加**: ベーストラックの末尾へ順次配置（重ならない位置に詰める）。
- **タイムラインへドラッグ＆ドロップ**: 落とした位置が空ならそこへ。埋まっていれば上位の空き `visual` トラックへ。無ければ最上段に新規 `visual` トラックを自動生成して配置。
- **テロップ追加**: 再生位置に置く。空いている最上位の `visual` トラックへ。無ければ最上段に新規トラックを生成。
- **SRT / 字幕取込**: 専用の「字幕」`visual` トラックを最上段に作り、まとめて配置。
- **縦ドラッグで最上段より上へ**: 視覚クリップを最上段トラックより上へドラッグすると、新しい最上位 `visual` トラックを自動生成して移動（1 回のドラッグにつき 1 つだけ。空のまま終わったトラックは自動で片付ける）。
- 音声クリップは `audio` トラック間のみ移動でき、`visual` 領域には入らない。

---

## 4. タイムライン操作

- **横ドラッグ** = クリップの移動。**端（トリムハンドル）ドラッグ** = イン/アウトのトリム。
- **縦ドラッグ** = トラック間移動（種別が合うトラックのみ）。最上段より上は新規トラック生成（§3）。
- **Alt + ドラッグ** = 複製してコピー側を移動（元は残す。別トラックへも複製可）。
- **スナップ**: 0 秒・再生位置・各クリップ端に吸着。
- **ズーム**: ボタン、トラックパッドのピンチ、Ctrl/⌘ + ホイール。「全体表示」ボタンあり。
- クリップの重なりは基本的に許容（同一トラック内は自動レイヤーで重ならないようにする）。

### コピー / 貼り付け / 複製

- `⌘/Ctrl + C` コピー、`⌘/Ctrl + V` 貼り付け、`⌘/Ctrl + X` 切り取り、`⌘/Ctrl + D` 複製。
- **貼り付け**は再生位置（カーソル）に配置。カーソルが元と同じ位置で重なる場合は **1 つ上のトラックへ繰り上げて**同じ時間に配置（上が埋まっていればさらに上、無ければ最上段に新規生成）。
- **複製**は元クリップの直後（右隣の空き）へ。

---

## 5. 合成ルール（プレビュー・書き出し共通）

- **下 → 上のトラック順**で合成する。上のトラックの**不透明クリップ（動画・画像）が、下のクリップをその重なり区間だけ隠す**（トラック優先合成。Premiere / Filmora と同様）。
- **テロップは透過オーバーレイ**（下の映像が透ける）。
- プレビューと書き出しの重なり順は一致する。
- プレビューは **キャンバスコンポジタ方式**: 不透明キャンバスが唯一の表示レイヤ。`<video>` はフレーム供給＆音声のデコーダとして裏で使用。GPU の動画合成に依存しないため確実に表示され、書き出しと見た目が一致する。
- 内蔵プレーヤーが復号できないコーデック（HEVC / ProRes 等）は **FFmpeg のフレーム抽出にフォールバック**して表示。

---

## 6. プレビュー上の操作

- **タップで選択**: プレビューをクリックすると、その位置のクリップが選択される。重なっている場合は **上の層が優先**。
- **ドラッグで位置移動**: テロップ・画像・動画（**ベース動画 V1 を含む**）はプレビュー上でドラッグして位置を動かせる（テロップは `x/y`、画像・動画は `transform.x/y`）。ベース動画を縮小・移動すると周囲は黒で埋まる。
- **四隅の丸ハンドルで大きさ変更**: 選択中クリップの四隅に丸いハンドルを表示。これをドラッグするとサイズが変わる（**動画・画像 = 拡大率**、**テロップ = 文字サイズ**）。中心からの距離比でスケールする。ベース動画も対象。
- 選択中クリップは破線枠＋四隅ハンドルで表示。ドラッグ中はインスペクタの「位置・サイズ」スライダーもリアルタイム同期。
- カーソルはクリップ本体上で `move`、四隅ハンドル上で `nwse-resize` になる。

---

## 7. 音声

- **ベース動画の音声**: 書き出しの「ベース連結」に含まれる。
- **非ベース（2 層目以降）の動画クリップの音声**: プレビュー再生・書き出しの両方でミックスされる（音声トラックを持つ素材のみ）。
- **音声トラックのクリップ**（BGM・ナレーション等）: `amix` でミックス。クリップごとに音量を持つ。
- 書き出し時はベース音声＋全レイヤー音声を `amix` で合成。

---

## 8. インスペクタ（プロパティ）

- **テロップ**: テキスト、デザインプリセット 8 種、アニメーション 9 種（none/フェード/スライド↑↓←→/ポップ/ズーム/タイプライター）、フォント、文字サイズ、太字・斜体、色、整列、フチ、背景帯、影。
- **動画・画像**: 配置（開始）・長さ、位置・サイズ（左右 X / 上下 Y / 拡大率）。
- **音声**: 配置・長さ、ボリューム。
- 「全テロップ選択」で一括編集（プリセット・アニメ等の一括適用、全削除）。

---

## 9. キーボードショートカット

| キー | 動作 |
| --- | --- |
| Space | 再生 / 停止 |
| ← / → | コマ送り（Shift で 1 秒）|
| Home / End | 先頭 / 末尾へ |
| S | 分割 |
| A | 再生位置より前をカット |
| D | 再生位置より後ろをカット |
| Backspace / Delete | 選択クリップを削除 |
| R | 範囲選択モード切替 |
| Esc | 選択モードへ戻す |
| ⌘/Ctrl + Z | 取り消し |
| ⌘/Ctrl + Shift + Z, ⌘/Ctrl + Y | やり直し |
| ⌘/Ctrl + C / V / X / D | コピー / 貼り付け / 切り取り / 複製 |
| ⌘/Ctrl + S | 保存（Shift で別名保存）|
| ⌘/Ctrl + O | プロジェクトを開く |
| ⌘/Ctrl + E | 書き出し |
| ⌘/Ctrl + I | 素材を取込 |
| ⌘/Ctrl + T | テロップ追加 |
| ⌘/Ctrl + B | 分割 |

> テキスト入力中は OS 標準のコピー/貼付/取消などを優先（単独キーのショートカットは無効）。

---

## 10. 入出力

- **取込**: 動画（mp4 / mov / m4v / webm / mkv / avi / mpg / ts）、画像（png / jpg / jpeg / webp / gif / bmp）、音声（mp3 / wav / m4a / aac / ogg / flac / aif）。ファイルのドラッグ＆ドロップ対応。
- **XML 取込**: FCP7 / Premiere の xmeml。
- **字幕取込**: SRT / WebVTT → テロップ化。
- **文字起こし**: ローカル Whisper（whisper-cli / whisper-cpp / openai-whisper を自動検出）で音声 → SRT → テロップ生成。
- **プロジェクト保存/読込**: `.tceproj`（JSON）。旧形式（v1）からの移行に対応。
- **書き出し**: システム FFmpeg を使用。ベースを「タイムライン順セグメントの連結」で構築（隙間は黒＋無音、全セグメント pts 0 始まりで overlay の framesync 問題を回避）。その上に**トラック順（下→上）でレイヤーを合成**: 非ベース動画は全尺の透過 RGBA 連結として overlay、テロップ・画像はフルフレーム透過 PNG として overlay（テロップはアニメ時にアルファをフェード）。音声は `amix`。出力は H.264 / AAC の mp4。

---

## 11. プロジェクト設定

- 解像度（既定 1280×720。プリセット＋カスタム）。
- フレームレート（既定 30fps）。

---

## 12. ホーム（スタート）画面

- 起動時は編集画面ではなく **ホーム画面**を表示する。
- アクション: **新規プロジェクト** / **プロジェクトを開く** / **素材から始める**（新規＋素材読込）。
- **最近のプロジェクト**一覧（名前・パス・最終日時）。クリックでそのまま開いて編集画面へ。
- 記録は `localStorage`（キー `telora.recents`、最大 12 件）に永続化。保存・読込のたびに追加。開けないファイルは一覧から除去。
- 編集画面のツールバー左の「🏠 ホーム」でいつでもホームに戻れる（未保存の変更があれば確認）。

---

## 13. エフェクト・アニメーション・編集拡張

- **変形エフェクト**（動画・画像、ベース動画含む）: 位置・拡大率に加え **回転**・**不透明度**・**クロップ（上下左右トリミング）**。
- **クロマキー**: 指定色（既定グリーン）を透過。類似度・境界ブレンド調整。プレビューは距離しきい値、書き出しは `colorkey`。
- **フェードイン/アウト**: クリップ単位（映像＝アルファ／音声＝音量）。
- **トランジション（クロスフェード）**: 選択クリップを直前のクリップに重ねてディゾルブ（上トラックへ移して fadeIn）。
- **速度変更**: 0.25〜4倍（スロー/早送り）。タイムライン尺＝素材尺/速度。プレビューは playbackRate、書き出しは setpts/atempo。
- **キーフレーム**: 位置・拡大率・回転・不透明度を時間変化（線形補間）。再生位置で値を編集すると自動でキーフレーム化。書き出しは時間サンプリングで反映。
- **複数選択**: Shift/Cmd クリックで追加選択。まとめて移動・削除・コピー/貼り付け・複製。
- **オーディオ波形**: 音声クリップに波形を表示（素材を一度デコードしてピークをキャッシュ）。

## 14. 表示補助・マーカー

- **マーカー**: 再生位置にマーカー（M キー/ボタン）。ルーラーに表示・クリックで移動。プロジェクトに保存。
- **ガイド**: セーフゾーン（外周5%/10%）・三分割グリッド・中央十字をプレビューに表示（トグル）。
- **静止画書き出し**: 現在のプレビューフレームを PNG 保存。

## 15. プレビュー高速化（プロキシ）

- 重い素材の **480p 低解像度プロキシ** を生成・キャッシュし、プレビュー再生に使用（書き出しは常にオリジナル）。トグルで切替。

## 16. 書き出しオプション（拡張）

- **画質**（高/標準/軽量＝CRF）、**形式**（MP4 / WebM / 音声MP3）、**ハードウェアエンコード**（VideoToolbox）。
- **範囲書き出し**: 範囲選択中はその区間のみ。
- **SRT 字幕書き出し**。

---

## 17. 用語

- **ベーストラック**: 最下段の `visual` トラック。主映像・音声・コーデックフォールバックの基準。
- **レイヤー（層）**: トラックの別称。上の層ほど前面。
- **テロップ**: 画面に重ねる文字（字幕）クリップ。
- **PIP**: 非ベース動画をベースの上に重ねる（ピクチャー・イン・ピクチャー）。
